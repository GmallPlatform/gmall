div(class=dj[key].wrapclass)
    div(class=mobileWrapper)
        div(class="container")
            div(class="text-two")
                div(class="text-box")
                    h3(ng-click="isCollapsed = !isCollapsed")=dj[key].name
                        span(ng-show="isCollapsed")
                            span(class="pull-right icon-delete-img")
                            div(class="clearfix")
                        span(ng-show="!isCollapsed")
                            span(class="pull-right icon-plus-img")
                            div(class="clearfix")
                div(uib-collapse="!isCollapsed")
                    p
                        | !{dj[key].desc}
                    //- var desc=dj[key].desc.clearTag().substring(0,250)

